plugin: aws_ec2
regions:
  - eu-west-1       # Adjust the region as per your requirements
  - eu-central-1
keyed_groups:
  - key: tags
    prefix: tag
compose:
  ansible_host: public_ip_address
filters:
  instance-state-name: ["running"]
